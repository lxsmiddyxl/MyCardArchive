/**
 * Critical above-the-fold hints for marketing pages (injected via layout class).
 * Full styles live in globals.css; this documents the minimal shell tokens.
 */
export const MARKETING_CRITICAL_CLASS = "mca-marketing-surface";

export const MARKETING_CRITICAL_CSS_HINTS = [
  "bg-mca-surface",
  "text-mca-ink-strong",
  "antialiased",
] as const;
