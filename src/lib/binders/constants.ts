/** Slots per binder page in the default 3×3 grid. */
export const BINDER_SLOTS_PER_PAGE = 9;

export const BINDER_GRID_COLS = 3;

/** Inclusive slot_index range per migration 032. */
export const BINDER_SLOT_INDEX_MIN = 0;
export const BINDER_SLOT_INDEX_MAX = 23;

/** Temp page number for two-phase swaps (must exceed any real page index). */
export const BINDER_PAGE_SWAP_TEMP = 999_999;
