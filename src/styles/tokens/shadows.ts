/**
 * Box shadows — shell elevation (see existing `shadow-mca-card` / `shadow-mca-panel`).
 */
export const shadows = {
  mcaCard:
    "0 1px 2px rgb(0 0 0 / 0.25), 0 4px 12px rgb(0 0 0 / 0.15)",
  mcaPanel: "0 1px 3px rgb(0 0 0 / 0.2)",
} as const;
