-- MyCardArchive v1.0.0-rc1 — documentation-only marker for the launch-track schema freeze.
-- No DDL changes beyond this COMMENT; deploy migrations through CI before tagging RC builds.
COMMENT ON SCHEMA public IS 'MyCardArchive RC1 (v1.0.0-rc1): treat public schema as launch-frozen unless a numbered migration explicitly extends it.';
